# MongoDB connection string (local or Atlas)
MONGODB_URI=mongodb+srv://vijaysuryawanshi1188_db_user:Sangamner123@cluster0.mc7f3tc.mongodb.net/?appName=Cluster0&authSource=admin

# Server port
PORT=5000

# Simple shared-secret auth between the VS Code extension and this backend.
# Replace with a real per-user auth system before you have multiple users.
API_KEY=dev-local-key-change-me

# NOTE: no AI API key needed here. Doc generation calls happen inside the
# VS Code extension via vscode.lm, using whatever chat model the developer
# already has access to (e.g. GitHub Copilot) - see extension/src/languageModel.ts
